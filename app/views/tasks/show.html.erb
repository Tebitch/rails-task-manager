<h4><%= @task.title %></h4>

<p>task detail</p>
<p><%= @task.details %></p>
<p><%= link_to "edit the task", edit_task_path(@task) %></p>
<%= link_to "back", tasks_path %>
<p><%= link_to "delete the task", task_path(@task),
method: :delete,
data: { confirm: "Are you sure?" } %></p>

